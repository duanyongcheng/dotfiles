## config tmux

```
ln -s ~/dotfiles/tmux ~/.config/tmux
mkdir ~/.config/tmux/plugins
git clone https://github.com/tmux-plugins/tpm.git ~/.config/tmux/plugins/tpm

Ctrl + A  | Shift + I  to instal plugins
```
